+ Each folder contains the notes and corresponding source code on a specific subject. Currently only the java notes is not too shabby.

+ Notes are written in *org mode*. Open them with a markdown tool(emacs + org-mode, or vscode + org-mode plugin) is suggested.
