+ Each folder contains the notes and corresponding source code on a specific subject. Currently only the java note is not too shabby.

+ Notes are written in *org mode*. Open them with a markdown tool(Emacs + org-mode, or VSCode + org-mode plugin) is recommended. Though effort will be made to provide a compatible display on the GitHub page in all future notes. 
